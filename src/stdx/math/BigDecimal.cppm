/**
 * @file BigDecimal.cppm
 * @module stdx:math.BigDecimal
 * @brief Implementation of the BigDecimal class.
 *
 * This file contains the implementation of the BigDecimal class, which is used to represent arbitrary-sized decimal values.
 */

module;

#if defined(STDLIBX_NO_RESERVED_STD_MODULE) || defined(DOXYGEN)
export module stdx:math.BigDecimal;
#else
export module stdlibx:math.BigDecimal;
#endif
