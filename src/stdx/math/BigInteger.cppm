/**
 * @file BigInteger.cppm
 * @module stdx:math.BigInteger
 * @brief Implementation of the BigInteger class.
 *
 * This file contains the implementation of the BigInteger class, which is used to represent arbitrary-sized integer values.
 */

module;

#if defined(STDLIBX_NO_RESERVED_STD_MODULE) || defined(DOXYGEN)
export module stdx:math.BigInteger;
#else
export module stdlibx:math.BigInteger;
#endif
